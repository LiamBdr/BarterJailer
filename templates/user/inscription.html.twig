{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form.css') }}">
{% endblock %}

{% block javascripts %}
{% endblock %}

{% block body %}

    {{ form_start(registrationForm) }}

        <h1>Inscription</h1>

        {{ form_row(registrationForm.username) }}

        <div class="form-names">
            {{ form_row(registrationForm.firstname) }}
            {{ form_row(registrationForm.lastname) }}
        </div>

        {{ form_row(registrationForm.email) }}

        <p>Avatar</p>
        <div class="form-avatars">
            {% for avatar in  registrationForm.avatar %}
                {%  if avatar.vars.value == 1 %}
                    {%  set src = asset('avatars/armand.png') %}
                    {%  set checked = 'checked' %}
                {%  elseif avatar.vars.value == 2 %}
                    {%  set src = asset('avatars/roberto.png') %}
                    {%  set checked = '' %}
                {% else %}
                    {%  set src = asset('avatars/armand.png')%}
                    {%  set checked = '' %}
                {% endif %}
                <div>
                    {{ form_widget(avatar) }}
{#                    {{ avatar.vars.checked }}#}
{#                    <input {{ checked }} type="radio" id="{{ avatar.vars.id }}" name="{{ avatar.vars.full_name }}" value="{{ avatar.vars.value }}" required>#}
                    <label for="{{ avatar.vars.id }}">
                        <img class="avatar" src="{{ src }}" alt="">
                        {{ avatar.vars.label }}
                    </label>
                </div>
            {% endfor %}
        </div>

        <div>
            {{ form_label (registrationForm.plainPassword.first) }}
            {{ form_widget (registrationForm.plainPassword.first) }}
        </div>

        <div>
            {{ form_label (registrationForm.plainPassword.second) }}
            {{ form_widget (registrationForm.plainPassword.second) }}
        </div>

        {{ form_row(registrationForm.captcha) }}

        <div class="form-checkbox">
            <p>Accepter les CGU</p>
            {{ form_widget(registrationForm.agreeTerms) }}
            {{ form_label(registrationForm.agreeTerms) }}
        </div>

        {{ form_rest(registrationForm) }}

        <div class="form-errors">
            {{ form_errors(registrationForm) }}
        </div>

        <button type="submit" class="btn">S'inscrire</button>

        <a href="{{ path('app_home') }}">Retour à l'accueil</a>
{% endblock %}

{% block additional_scripts %}
    {# GOOGLE CAPTCHA #}
    <script type="text/javascript" src="{{ asset('js/captcha.js') }}"></script>
    <script type="text/javascript" src="https://www.google.com/recaptcha/api.js?onload=onGoogleReCaptchaApiLoad&render=explicit&hl={{app.request.locale}}" async defer></script>
{% endblock %}